/** @type {import('next').NextConfig} */
const nextConfig={
    images:{
        remotePatterns:[
            {hostname:"calculating-flamingo-659.convex.cloud"}
        ],
    },
};
export default nextConfig;